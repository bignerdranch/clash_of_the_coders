<%= form_for @team do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.collection_select(:member_ids, available_users, :id, :name, {prompt: 'Select team members' }, {:multiple => true}) %>

  <%= f.submit %>
<% end %>
