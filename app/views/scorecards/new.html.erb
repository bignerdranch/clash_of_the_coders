<div class="row">
  <div class="columns large-10 large-centered">
    <h1 class="centered">Vote</h1>
    <%- if dynamic_users? %>
      <p>Hi, <%= current_user.name %></p>
    <% end %>

    <p>Vote for all the teams below.</p>
    <p>Voting rules:</p>
    <ol>
      <li>You must vote for every team.</li>
      <li>
        You can award each team <b>up to <%= @scorecard.max_score %></b>
        points. That's up to <b><%= @scorecard.max_learning_score %></b> points (displayed at demo site)
        for using next-gen server tech, and up to <b><%=
          @scorecard.max_wizardry_score %></b> points for overall technical wizardry.

      </li>
     <li>A team's final score is the average of all their votes plus
       the bonus 7 points for cross-team collaboration if earned.</li>
    </ol>

    <%= render 'scorecards/form', scorecard: @scorecard %>
  </div>
</div>
