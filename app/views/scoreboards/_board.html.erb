<table>
  <thead>
    <tr>
      <th>Position</th>
      <th>Team name</th>
      <th>Score</th>
    </tr>
  </thead>
  <tbody>
    <%- teams.each do |team| %>
      <tr id='<%= dom_id(team) %>'>
        <td><%= team.position %></td>
        <td><%= team.name %></td>
        <td><%= team.score.round(2) %><%= '*' if team.bonus? %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p>* - team qualifies for 7 point cross-collaboration bonus.</p>
